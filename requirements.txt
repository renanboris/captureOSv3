# Runtime dependencies for CaptureOS v3.
#   pip install -r requirements.txt
# Dev/test-only deps live in requirements-dev.txt.

# --- Web framework / server ---
fastapi==0.109.2
uvicorn==0.27.1
# pydantic must satisfy google-genai (>=2.9.0) and pydantic-settings (>=2.7.0).
pydantic==2.12.5
pydantic-settings==2.14.1
python-multipart==0.0.9

# --- Configuration ---
python-dotenv==1.0.1

# --- AI / RAG ---
google-genai==1.69.0
openai>=1.13.3
pinecone-client==3.1.0

# --- Capture / vision ---
# som_annotator.py imports playwright at module load (transitively via api.main).
playwright==1.58.0
Pillow>=10.2.0

# --- Audio / video pipeline ---
edge-tts==6.1.9
ffmpeg-python==0.2.0
# time_bender.py injects a bundled FFmpeg/FFprobe at import time.
static-ffmpeg==3.0

# --- Artifacts ---
reportlab==4.1.0

# --- HTTP clients ---
aiohttp==3.9.3
# lms_callback.py uses httpx for the LMS completion callback (simlink conclusao).
httpx==0.28.1
